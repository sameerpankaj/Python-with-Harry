a = (1,) # Creating a tuple with a single element. Note the comma after 1, which is necessary to indicate that it is a tuple.)
print(type(a)) # Output: <class 'int'>, because it is not a tuple, it is just an integer.   
