a = (1, 2, 5, 6) # Creating a tuple with 4 elements.
print(type(a)) # Output: (1, 2, 5, 6)
#tuple is a collection data type that is ordered and immutable. It is defined using parentheses () and can contain elements of different data types. Once a tuple is created, its contents cannot be modified, which means you cannot add, remove, or change elements in a tuple after it has been created.

