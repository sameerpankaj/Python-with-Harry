#The index in a string starts from 0 to (lenght -1) in Python. In order to slice a string, we use th following syntax:

name = 'Harry' #Strings are immutable

nameshort = name[0:3] # start from index 0 all the way till 3 (excluding 3)

print(nameshort)

character1 = name[1]
print(character1)