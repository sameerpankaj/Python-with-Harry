#check the type of variable assigned using input() function

a = input('Enter the value of a:')
print(type(a))

#This will always give str type
#to get the desired data type, we need to use the data type at the starting of the input functionality
